// Barrel: typed deck content. The 8-section metadata (sections.ts) consumed by
// the deck-player jump menu + counter. The ordered slide registry lives next to
// the slide components in `src/app/deck/slides/`.
export { deckSections } from "./sections";

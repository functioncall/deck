// Barrel: bespoke atoms built from deck tokens + Radix primitives (ADR-0005).
// Text & structural atoms (L1 epic-0). Interactive + code/data atoms land in
// epic-1; this barrel is extended, never rewritten.
export { Heading } from "./Heading";
export { Text } from "./Text";
export { Label } from "./Label";
export { Link } from "./Link";
export { Badge } from "./Badge";

// Barrel: landing-page sections — organism-level composites that read editorial
// copy from typed content (`@/content`) and reuse L1 organisms + L2 diagrams to
// render the SPEC §3 anatomy. Assembled into `/` in epic-3. Top-of-page narrative
// (Hero → Problem → The Shift → Deck preview → What's in the Kit) ships in epic-1;
// the conversion sections (Proof → Pricing → FAQ → Final CTA) extend it in epic-2.
export { Hero } from "./Hero";
export { Problem } from "./Problem";
export { TheShift } from "./TheShift";
export { DeckPreview } from "./DeckPreview";
export { WhatsInTheKit } from "./WhatsInTheKit";
